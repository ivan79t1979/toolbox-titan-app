'use client';

// This file is no longer used, but is kept to prevent breaking imports.
// The functionality is now provided by the embedded Gradio app.

export function BackgroundRemoverForm() {
  return null;
}
